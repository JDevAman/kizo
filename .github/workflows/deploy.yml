name: 🚀 Kizo Production Deploy

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  # --- STAGE 1: TEST ---
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          # Removed the npm cache requirement since we don't have a lockfile yet

      - name: Install Deps
        run: npm ci

      - name: Prepare Workspace
        run: |
          npm run prepare:backend
          npx turbo build --filter="@kizo/db" --filter="@kizo/logger" --filter="@kizo/queue" --filter="@kizo/metrics" --filter="@kizo/shared"
        env:
          DATABASE_URL: "postgresql://postgres:password@localhost:5432/postgres"
          DIRECT_URL: "postgresql://postgres:password@localhost:5432/postgres"
          REDIS_URL: "redis://localhost:6379"

      - name: Run Vitest
        run: npm test

  # --- STAGE 2: BUILD & PUSH (MONOLITH) ---
  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Docker Login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          
      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: haribhakt/kizo:latest

  # --- STAGE 3: DEPLOY ---
  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Copy docker-compose.prod.yml
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.GCP_VM_IP }}
          username: ${{ secrets.GCP_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "docker-compose.prod.yml" # Make sure your docker-compose is named prod.yml in your repo!
          target: "~/kizo"
          
      - name: SSH Deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.GCP_VM_IP }}
          username: ${{ secrets.GCP_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            mkdir -p ~/kizo
            echo "${{ secrets.PROD_ENV_FILE }}" > ~/kizo/.env.prod
            cd ~/kizo
            docker compose -f docker-compose.prod.yml pull
            docker compose -f docker-compose.prod.yml up -d
            docker system prune -f